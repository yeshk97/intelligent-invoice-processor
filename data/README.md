# Data Directory

This directory contains:

## `sample_invoices/`
Example invoice PDFs and images for testing and development.

## `schemas/`
Database schema definitions and migration scripts.

## Usage
- Add sample invoices to `sample_invoices/` for testing
- SQL schema files go in `schemas/`
- Never commit real customer data - use anonymized samples only
